import Lottie from 'lottie-react';
import data from './Files/Login-and-Signup.json';

const Forms = () => {
  return (
    <Lottie width="100%" height="100%" animationData={data} loop={false} />
  );
};

export default Forms;
